```python3 -m venv .venv ```
in linux:
```source .venv/bin/activate```
in windows:
```activate```
Make sure to select the venv in the python interpreter
```pip install -r requirements.txt```
```python manage.py runserver```